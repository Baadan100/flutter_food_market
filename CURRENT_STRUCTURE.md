# البنية الحالية للمشروع 📁

## 📂 هيكل الملفات الحالي

```
flutter_food_market/
├── lib/
│   ├── app/
│   │   └── router.dart                    # التوجيه (go_router) - بسيط جداً
│   │
│   ├── core/                              # غير موجود حالياً - سيتم إنشاؤه
│   │
│   ├── features/
│   │   ├── auth/
│   │   │   ├── application/
│   │   │   │   └── auth_controller.dart   # Controller بسيط (وهمي)
│   │   │   ├── domain/
│   │   │   │   └── app_user.dart          # نموذج بسيط (email فقط)
│   │   │   └── presentation/
│   │   │       └── auth_pages.dart        # صفحات تسجيل الدخول والتسجيل
│   │   │
│   │   ├── catalog/
│   │   │   ├── data/
│   │   │   │   ├── sample_products.dart   # 12 منتج تجريبي
│   │   │   │   └── sample_bundles.dart    # 2 وليمة تجريبية
│   │   │   ├── domain/
│   │   │   │   ├── product.dart           # نموذج المنتج
│   │   │   │   └── bundle.dart            # نموذج الوليمة
│   │   │   └── presentation/
│   │   │       └── catalog_page.dart      # صفحة المنتجات + السلة
│   │   │
│   │   ├── cart/
│   │   │   ├── application/
│   │   │   │   └── cart_controller.dart   # Controller كامل ويعمل
│   │   │   └── domain/
│   │   │       └── cart_item.dart         # نموذج عنصر السلة
│   │   │
│   │   ├── checkout/
│   │   │   └── payment_service.dart        # غير مستخدم (معلق)
│   │   │
│   │   ├── home/
│   │   │   └── home_page.dart             # صفحة رئيسية بسيطة
│   │   │
│   │   ├── onboarding/
│   │   │   └── onboarding_page.dart       # صفحة التعريف
│   │   │
│   │   ├── settings/
│   │   │   ├── locale_controller.dart     # Controller اللغة
│   │   │   └── theme_controller.dart      # Controller الثيم
│   │   │
│   │   ├── shell/
│   │   │   ├── app_drawer.dart            # القائمة الجانبية
│   │   │   └── shell_scaffold.dart        # الهيكل الرئيسي + Bottom Nav
│   │   │
│   │   └── splash/
│   │       └── splash_page.dart           # شاشة البداية
│   │
│   ├── l10n/
│   │   └── app_localizations.dart         # نظام الترجمة
│   │
│   ├── theme/
│   │   └── app_text_styles.dart           # أنماط النصوص
│   │
│   ├── widgets/
│   │   └── glass.dart                     # تأثير Glassmorphism
│   │
│   └── main.dart                          # نقطة البداية
│
├── assets/
│   ├── images/
│   │   └── products/                      # صور المنتجات (12 صورة)
│   └── translations/
│       ├── ar.json                        # الترجمة العربية (محدودة)
│       └── en.json                        # الترجمة الإنجليزية (محدودة)
│
├── functions/
│   ├── src/
│   │   └── index.ts                       # Firebase Functions (غير مكتمل)
│   └── package.json                       # Dependencies
│
├── android/
│   └── app/
│       ├── google-services.json           # موجود (Firebase config)
│       └── src/
│           └── main/
│               └── AndroidManifest.xml
│
└── pubspec.yaml                           # Dependencies
```

---

## 🔍 تحليل المكونات الحالية

### ✅ ما يعمل بشكل جيد:

#### 1. **نظام السلة (Cart)**
- ✅ `CartController` - إدارة كاملة للحالة
- ✅ إضافة/حذف/تعديل الكمية
- ✅ حساب الإجمالي
- ✅ يعمل مع Riverpod بشكل صحيح

#### 2. **نظام الترجمة**
- ✅ دعم العربية والإنجليزية
- ✅ RTL/LTR يعمل
- ✅ `LocaleController` للتبديل

#### 3. **نظام الثيم**
- ✅ الوضع الداكن/الفاتح
- ✅ `ThemeController` للتبديل
- ✅ Material Design 3

#### 4. **التصميم**
- ✅ Glassmorphism effects
- ✅ Material Design 3
- ✅ Responsive design

---

### ⚠️ ما يحتاج تطوير:

#### 1. **نظام المصادقة (Auth)**
**الحالة الحالية:**
```dart
// auth_controller.dart - وهمي تماماً
Future<void> signIn(String email, String password) async {
  if (email.isEmpty || password.length < 4) {
    state = const AuthState(errorMessage: 'بيانات غير صالحة');
    return;
  }
  state = AuthState(user: AppUser(email: email)); // وهمي!
}
```

**ما يحتاج:**
- ❌ ربط مع Firebase Auth
- ❌ التحقق من البريد الإلكتروني
- ❌ إعادة تعيين كلمة المرور
- ❌ حفظ حالة المستخدم
- ❌ تحديث `AppUser` لإضافة المزيد من الحقول

#### 2. **قاعدة البيانات**
**الحالة الحالية:**
- ✅ بيانات تجريبية في `sample_products.dart`
- ❌ لا يوجد اتصال بقاعدة بيانات حقيقية
- ❌ لا يوجد Repository pattern

**ما يحتاج:**
- ❌ Firestore integration
- ❌ Product Repository
- ❌ Category Repository
- ❌ Order Repository
- ❌ User Repository

#### 3. **الصفحة الرئيسية**
**الحالة الحالية:**
```dart
// home_page.dart - بسيطة جداً
- مربع بحث (غير مفعّل)
- صورة واحدة ثابتة
- قائمة أفقية للمنتجات
```

**ما يحتاج:**
- ❌ Carousel للصور المتحركة
- ❌ أقسام منظمة (أسماك، ولائم، عروض)
- ❌ عداد خصم
- ❌ بحث فعلي

#### 4. **نظام الطلبات**
**الحالة الحالية:**
- ❌ لا توجد صفحة Checkout
- ❌ لا توجد صفحة Orders
- ❌ لا يوجد Order model
- ❌ لا يوجد Order Controller

#### 5. **لوحة التحكم**
**الحالة الحالية:**
- ❌ غير موجودة تماماً

#### 6. **نظام الرسائل**
**الحالة الحالية:**
- ❌ لا يوجد نظام رسائل
- ❌ Firebase Functions غير مكتمل

---

## 📊 البيانات الحالية

### المنتجات (12 منتج):
1. سلمون طازج - 35 ر.س
2. روبيان مقشّر - 28 ر.س
3. تونة شرائح - 32 ر.س
4. إسقمري - 19 ر.س
5. سردين - 12 ر.س
6. سرطان البحر - 45 ر.س
7. كركند - 90 ر.س
8. أخطبوط - 52 ر.س
9. دنيس - 26 ر.س
10. قاروص - 30 ر.س
11. انشوجة - 15 ر.س
12. بلطي - 17 ر.س

### الولائم (2 وليمة):
- وليمة عائلية
- وليمة حفلة

---

## 🔧 التبعيات الحالية

```yaml
dependencies:
  flutter_riverpod: ^2.6.1        # ✅ State Management
  go_router: ^15.1.2              # ✅ Routing
  freezed_annotation: ^2.4.1      # ✅ Code Generation
  json_annotation: ^4.9.0         # ✅ JSON
  dio: ^5.9.0                     # ✅ HTTP
  retrofit: ^4.5.0                # ✅ API Client
  flutter_secure_storage: ^9.2.4  # ✅ Secure Storage
  intl: ^0.19.0                   # ✅ Internationalization
  cached_network_image: ^3.4.1    # ✅ Image Caching
  shimmer: ^3.0.0                 # ✅ Loading Shimmer
  flutter_localizations:          # ✅ Localization
```

**ملاحظة:** لا توجد تبعيات Firebase حالياً!

---

## 🎯 الخطوات التالية المباشرة

### 1. إضافة تبعيات Firebase
```bash
flutter pub add firebase_core firebase_auth cloud_firestore firebase_storage firebase_messaging firebase_functions
```

### 2. تحديث ملفات الترجمة
- إضافة جميع النصوص المطلوبة
- دعم كامل للغتين

### 3. إنشاء Core Layer
- Constants
- Errors
- Utils
- Services

### 4. تطوير Repository Pattern
- Product Repository
- Category Repository
- Order Repository

---

## 📝 ملاحظات مهمة

1. **الكود الحالي نظيف ومنظم** - Clean Architecture
2. **Riverpod يعمل بشكل جيد** - State Management جيد
3. **التصميم احترافي** - Material Design 3 + Glassmorphism
4. **الترجمة موجودة** - لكن تحتاج توسيع
5. **لا يوجد Firebase** - يحتاج إعداد كامل

---

**آخر تحديث:** 2025-01-11
