# دليل المساهمة - Flutter Fish Market

نرحب بمساهماتكم في تطوير مشروع سوق الأسماك! هذا الدليل سيساعدكم على المساهمة بشكل فعال.

## 🚀 كيفية البدء

### 1. إعداد البيئة
```bash
# استنساخ المشروع
git clone https://github.com/your-username/food_market.git
cd food_market

# تثبيت الاعتمادات
flutter pub get

# تشغيل التطبيق للتأكد من عمله
flutter run
```

### 2. إنشاء فرع جديد
```bash
git checkout -b feature/your-feature-name
# أو
git checkout -b fix/issue-description
```

## 📋 أنواع المساهمات

### 🐛 إصلاح الأخطاء
- ابحث عن الأخطاء في [Issues](https://github.com/your-username/food_market/issues)
- أنشئ فرع جديد للإصلاح
- اختبر الإصلاح جيداً
- أرسل Pull Request

### ✨ إضافة ميزات جديدة
- ناقش الميزة في Issues أولاً
- تأكد من أنها تتماشى مع أهداف المشروع
- اتبع معايير الكود الموجودة
- أضف اختبارات للميزة الجديدة

### 📚 تحسين الوثائق
- تحسين README
- إضافة تعليقات للكود
- كتابة دليل المستخدم
- ترجمة الوثائق

### 🎨 تحسينات التصميم
- تحسين واجهة المستخدم
- إضافة تأثيرات بصرية
- تحسين تجربة المستخدم
- دعم المزيد من الأجهزة

## 📝 معايير الكود

### تسمية الملفات
```dart
// ✅ صحيح
user_profile_page.dart
auth_controller.dart
product_model.dart

// ❌ خطأ
UserProfilePage.dart
authController.dart
ProductModel.dart
```

### تسمية المتغيرات
```dart
// ✅ صحيح
final String userName;
final int productCount;
final bool isLoggedIn;

// ❌ خطأ
final String UserName;
final int product_count;
final bool isloggedin;
```

### تنسيق الكود
```dart
// ✅ صحيح
class UserProfilePage extends ConsumerWidget {
  const UserProfilePage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('الملف الشخصي'),
      ),
      body: const Center(
        child: Text('مرحباً بك'),
      ),
    );
  }
}
```

### التعليقات
```dart
/// صفحة عرض الملف الشخصي للمستخدم
/// تدعم عرض المعلومات الشخصية وتعديلها
class UserProfilePage extends ConsumerWidget {
  // متغيرات الحالة
  final String userId;
  
  /// بناء الصفحة مع تمرير معرف المستخدم
  const UserProfilePage({
    super.key,
    required this.userId,
  });
}
```

## 🧪 الاختبار

### اختبارات الوحدة
```dart
// test/unit/auth_controller_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:food_market/features/auth/application/auth_controller.dart';

void main() {
  group('AuthController', () {
    test('should sign in with valid credentials', () async {
      // Arrange
      final controller = AuthController();
      
      // Act
      await controller.signIn('test@example.com', 'password');
      
      // Assert
      expect(controller.isSignedIn, true);
    });
  });
}
```

### اختبارات الواجهة
```dart
// test/widget/sign_in_page_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:food_market/features/auth/presentation/auth_pages.dart';

void main() {
  testWidgets('SignInPage should display email and password fields', (tester) async {
    // Arrange
    await tester.pumpWidget(const MaterialApp(home: SignInPage()));
    
    // Act & Assert
    expect(find.byType(TextField), findsNWidgets(2));
    expect(find.text('البريد الإلكتروني'), findsOneWidget);
    expect(find.text('كلمة المرور'), findsOneWidget);
  });
}
```

## 📤 إرسال التغييرات

### 1. Commit التغييرات
```bash
# إضافة الملفات
git add .

# Commit مع رسالة واضحة
git commit -m "feat: add user profile page with edit functionality"

# أو للإصلاحات
git commit -m "fix: resolve navigation issue in cart page"
```

### 2. Push للفرع
```bash
git push origin feature/your-feature-name
```

### 3. إنشاء Pull Request
- اذهب إلى GitHub
- انقر على "New Pull Request"
- اكتب وصفاً واضحاً للتغييرات
- أضف لقطات شاشة إذا لزم الأمر
- اطلب مراجعة من المطورين

## 📋 قالب Pull Request

```markdown
## 📝 الوصف
وصف مختصر للتغييرات المضافة

## 🔧 نوع التغيير
- [ ] إصلاح خطأ
- [ ] ميزة جديدة
- [ ] تحسين في الأداء
- [ ] تحديث الوثائق
- [ ] تحسين التصميم

## 🧪 الاختبار
- [ ] تم اختبار التغييرات محلياً
- [ ] تم إضافة اختبارات جديدة
- [ ] جميع الاختبارات تمر بنجاح

## 📱 لقطات الشاشة
(إذا كانت التغييرات تؤثر على الواجهة)

## ✅ قائمة التحقق
- [ ] الكود يتبع معايير المشروع
- [ ] تم تحديث الوثائق إذا لزم الأمر
- [ ] لا توجد أخطاء في التحليل
- [ ] التغييرات متوافقة مع الإصدارات السابقة
```

## 🏷️ تسمية Commits

استخدم البادئات التالية:

- `feat:` - ميزة جديدة
- `fix:` - إصلاح خطأ
- `docs:` - تحديث الوثائق
- `style:` - تحسينات في التنسيق
- `refactor:` - إعادة هيكلة الكود
- `test:` - إضافة أو تعديل الاختبارات
- `chore:` - مهام الصيانة

## 🐛 الإبلاغ عن الأخطاء

### قالب Bug Report
```markdown
## 🐛 وصف الخطأ
وصف واضح ومختصر للخطأ

## 🔄 خطوات إعادة الإنتاج
1. اذهب إلى '...'
2. انقر على '...'
3. مرر لأسفل إلى '...'
4. شاهد الخطأ

## 📱 البيئة
- الجهاز: [مثل iPhone 12]
- نظام التشغيل: [مثل iOS 15.0]
- إصدار التطبيق: [مثل 1.0.0]

## 📸 لقطات الشاشة
(إذا كانت مناسبة)

## 📋 معلومات إضافية
أي معلومات أخرى مفيدة
```

## 💡 اقتراح الميزات

### قالب Feature Request
```markdown
## 💡 الميزة المقترحة
وصف واضح للميزة المطلوبة

## 🎯 المشكلة التي تحلها
وصف المشكلة التي تحلها هذه الميزة

## 💭 الحل المقترح
وصف مفصل للحل المقترح

## 🔄 البدائل
وصف أي حلول بديلة تم التفكير فيها

## 📋 معلومات إضافية
أي معلومات أخرى مفيدة
```

## 📞 التواصل

- **البريد الإلكتروني**: [contact@lorienx.com](mailto:contact@lorienx.com)
- **الموقع الرسمي**: [lorienx.com](https://lorienx.com)
- **GitHub Issues**: للمناقشات التقنية

## 🙏 شكر وتقدير

نقدر مساهماتكم ونشكركم على دعم المشروع!

---

**تم إعداد هذا الدليل بـ ❤️ لضمان أفضل تجربة تطوير**
