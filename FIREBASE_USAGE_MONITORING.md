# مراقبة استخدام Firebase - Spark Plan

## 📊 كيفية مراقبة الاستخدام

### 1. في Firebase Console

#### خطوات الوصول:
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك: `seafood-marketplace-c98ae`
3. من القائمة الجانبية: **Usage and billing**

#### ما يجب مراقبته:

**Firestore:**
- Reads (قراءات): الحد 50K/يوم
- Writes (كتابات): الحد 20K/يوم
- Storage (تخزين): الحد 1GB

**Storage:**
- Storage used: الحد 5GB
- Downloads: الحد 1GB/يوم

**Authentication:**
- Active users: الحد 10K/شهر

---

## ⚠️ إعداد الإشعارات

### إعداد Budget Alert:
1. **Project Settings** → **Usage and billing**
2. اضغط **Set budget alert**
3. حدد المبلغ (مثلاً $0.50 أو $1)
4. سيتم إرسال إشعار عند:
   - الاقتراب من الحد
   - تجاوز الميزانية

---

## 💡 نصائح لتقليل الاستخدام

### 1. تقليل قراءات Firestore:
```dart
// ❌ سيء - يجلب كل مرة
final products = await repository.fetchProducts();

// ✅ جيد - يستخدم Stream مع Caching
final productsAsync = ref.watch(productsProvider);
```

### 2. استخدام Pagination:
```dart
// للقوائم الطويلة
.limit(20) // جلب 20 منتج فقط
```

### 3. ضغط الصور:
```dart
// في ImagePicker
imageQuality: 85, // بدلاً من 100
```

---

## 📈 تقدير الاستخدام

### سيناريو: 50 مستخدم نشط/يوم

**Firestore Reads:**
- كل مستخدم يفتح التطبيق 3 مرات/يوم
- كل فتحة = ~50 قراءة (قائمة المنتجات)
- الإجمالي: 50 × 3 × 50 = **7,500 قراءة/يوم** ✅ (أقل من 50K)

**Storage:**
- 100 منتج × 500KB = **50MB** ✅ (أقل بكثير من 5GB)

**Authentication:**
- 50 مستخدم/يوم × 30 يوم = **1,500 مستخدم/شهر** ✅ (أقل من 10K)

---

## ✅ الخلاصة

**مشروعك آمن مع Spark Plan إذا:**
- عدد المستخدمين < 100 مستخدم نشط/يوم
- عدد المنتجات < 500 منتج
- حجم الصور < 1GB

**راقب الاستخدام أسبوعياً** في Firebase Console!
